# Summon secrets configuration for Debate Generator
# Uses gcloud plugin to fetch secrets from GCP Secret Manager
#
# Usage: summon -p gcloud docker-compose up
#
# Format: !var <project-id> <secret-name> <version>

GCP_PROJECT_ID: debate-480911
ANTHROPIC_API_KEY: !var debate-480911 anthropic-api-key latest
